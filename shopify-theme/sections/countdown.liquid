{%- comment -%}
  Countdown — standalone full-width section.

  Renders the shared snippet 'countdown-card'. The same countdown is also
  available as an inline BLOCK inside the Main Song Product section, and as the
  global overlay via snippets/countdown-widget.liquid. All three share
  snippets/countdown-card.liquid + assets/lts-countdown.{css,js}.

  QA: ?now=<ISO> fakes the clock.
{%- endcomment -%}

{% render 'countdown-card',
  design: section.settings.design,
  heading: section.settings.heading,
  target_month: section.settings.target_month,
  target_day: section.settings.target_day,
  reference_tz: section.settings.reference_tz,
  recurring: section.settings.recurring,
  end_behavior: section.settings.end_behavior,
  end_message: section.settings.end_message,
  grace_days: section.settings.grace_days,
  cta_label: section.settings.cta_label,
  cta_link: section.settings.cta_link,
  transparent_bg: section.settings.transparent_bg
%}

{% schema %}
{
  "name": "Countdown",
  "tag": "section",
  "class": "lts-cd-section",
  "settings": [
    {
      "type": "select",
      "id": "design",
      "label": "Design",
      "default": "xmas-globe",
      "options": [
        { "group": "Christmas", "value": "xmas-advent", "label": "Christmas — Advent Cards" },
        { "group": "Christmas", "value": "xmas-globe", "label": "Christmas — Snow Globe" },
        { "group": "Christmas", "value": "xmas-sleigh", "label": "Christmas — Sleigh" },
        { "group": "Halloween", "value": "hall-jack", "label": "Halloween — Jack-o'-Lantern" },
        { "group": "Halloween", "value": "hall-neon", "label": "Halloween — Spooky Neon" },
        { "group": "Halloween", "value": "hall-cauldron", "label": "Halloween — Cauldron" }
      ]
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading (optional)"
    },
    {
      "type": "header",
      "content": "Countdown target"
    },
    {
      "type": "number",
      "id": "target_month",
      "label": "Target month",
      "default": 12,
      "info": "Month the countdown ends (1–12). E.g. 12 for December."
    },
    {
      "type": "number",
      "id": "target_day",
      "label": "Target day",
      "default": 25,
      "info": "Day of that month the countdown reaches zero. The timer hits 00:00:00 at the start of this day in the reference timezone below."
    },
    {
      "type": "select",
      "id": "reference_tz",
      "label": "Reference timezone",
      "default": "America/New_York",
      "info": "The countdown hits zero at midnight in this timezone, so every visitor sees exactly the same numbers regardless of where they are.",
      "options": [
        { "value": "America/New_York", "label": "New York (Eastern)" },
        { "value": "America/Chicago", "label": "Chicago (Central)" },
        { "value": "America/Denver", "label": "Denver (Mountain)" },
        { "value": "America/Los_Angeles", "label": "Los Angeles (Pacific)" },
        { "value": "UTC", "label": "UTC / London (GMT)" },
        { "value": "Europe/London", "label": "London (UK)" },
        { "value": "Pacific/Auckland", "label": "Auckland (New Zealand)" },
        { "value": "Australia/Sydney", "label": "Sydney (Australia)" }
      ]
    },
    {
      "type": "checkbox",
      "id": "recurring",
      "label": "Repeat every year",
      "default": true
    },
    {
      "type": "header",
      "content": "When the countdown reaches zero"
    },
    {
      "type": "select",
      "id": "end_behavior",
      "label": "At zero",
      "default": "message",
      "options": [
        { "value": "hide", "label": "Hide" },
        { "value": "message", "label": "Show message" },
        { "value": "message-then-hide", "label": "Show message then hide" }
      ]
    },
    {
      "type": "inline_richtext",
      "id": "end_message",
      "label": "End message",
      "default": "It's here!"
    },
    {
      "type": "number",
      "id": "grace_days",
      "label": "Grace days",
      "default": 6,
      "info": "For \"Show message then hide\": how many days the message stays up after zero before it hides. Ignored for the other modes and when \"Repeat every year\" is on."
    },
    {
      "type": "header",
      "content": "Call to action"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label",
      "default": "See the countdown"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "Button link",
      "info": "The button only appears when a link is set."
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "checkbox",
      "id": "transparent_bg",
      "label": "Transparent background",
      "default": false,
      "info": "Removes the skin's coloured background panel so it sits on your page's own background."
    }
  ],
  "presets": [
    {
      "name": "Countdown"
    }
  ]
}
{% endschema %}
